# Frontend Docker (.env.docker)
# Since we use Nginx proxy in Docker, the frontend talks to its own host at /api
VITE_API_URL=/api

# Backend Docker (.env.docker)
QDRANT_HOST=qdrant
QDRANT_PORT=6333
GROQ_API_KEY=